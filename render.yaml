services:
  - type: web
    name: scalable-ml-pipeline
    runtime: python
    buildCommand: pip install -r starter/requirements.txt && pip install -e starter/
    startCommand: cd starter && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
